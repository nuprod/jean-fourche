<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_user_connector_form" model="ir.ui.view">
        <field name="name">res.user.connector.form</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form" />
        <field name="arch" type="xml">
            <xpath expr="/form/sheet/notebook" position="inside">
                <page string="E-Commerce Integrations" name="connector">
                    <group string="Notifications">
                        <group>
                            <field name="notify_failed_jobs" />
                        </group>
                    </group>
                    <div class="alert alert-info" role="alert">
                        <strong>Note:</strong> Enabling this checkbox alone is not enough to receive notifications.
                        You must also have <strong>Job Queue Manager</strong> access rights to be enabled in the user permissions.
                    </div>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
